# AGENTS.md

## 项目原则

- 默认使用中文维护用户可见文档；公共接口、Schema 和代码标识使用英文。
- `packages/core/` 是 Skill 模型、概念注册表和状态逻辑的唯一事实入口，适配层不重复实现管理逻辑。
- Agent 适配器必须保持薄：不得各自实现一套 Skill 创建、禁用或分类逻辑。
- 概念注册表（core concepts）集中维护，Meta-Skill 生成 Skill 时必须以它为事实来源，不硬编码。
- 禁用不等于删除：禁用只是从 Agent 发现路径中摘除，文件保留。
- 禁止把聊天记录当作 Skill 或平台状态的唯一存储。

## 公共仓库安全

- 禁止提交真实用户 Skill 内容、凭据、含隐私信息的本地路径。
- 示例只能使用占位符。

## 交付边界

- 当前阶段是产品定义和可验证架构，不把文档草案描述成已实现功能。
