[package]
name = "skill-helm-desktop"
version = "0.1.0"
edition = "2021"
description = "Skill Helm 桌面端"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-updater = "2"
serde_json = "1"
