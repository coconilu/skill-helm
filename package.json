{
  "name": "skill-helm",
  "private": true,
  "packageManager": "pnpm@10.18.3",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r build && pnpm -r test",
    "lint": "biome ci .",
    "skill-helm": "node packages/cli/dist/cli.js"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.13"
  }
}
