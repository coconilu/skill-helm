{
  "name": "skill-helm",
  "version": "0.1.0",
  "private": true,
  "description": "Skill Helm 命令行：人与 Agent 共用的 Skill 管理入口",
  "bin": {
    "skill-helm": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/cli.js",
    "test": "vitest run"
  },
  "dependencies": {
    "@skill-helm/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
